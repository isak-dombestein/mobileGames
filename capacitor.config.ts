import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dombesteindata.coffeerun',
  appName: 'Coffee Run',
  webDir: 'dist',
};

export default config;
